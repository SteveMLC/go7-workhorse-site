# Production GA4 stream. The site silently drops analytics if this is missing
# at build time. Local builds without it print a warning.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-VCG5F1M7MB

# Optional. Point the live monitor at a preview deploy without editing code.
# MONITOR_URL=https://preview-go7workhorse.vercel.app
